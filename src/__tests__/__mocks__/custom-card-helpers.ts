// Minimal stub for the custom-card-helpers package used in unit tests.
export type HomeAssistant = Record<string, unknown>;
export type LovelaceCardEditor = Record<string, unknown>;
export type FrontendLocaleData = { language: string; number_format: string };
export const fireEvent = () => {};
export const formatNumber = () => "";
